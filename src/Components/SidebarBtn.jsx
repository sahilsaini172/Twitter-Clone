export default function SidebarBtn({onClick}) {
  return (
    <div onClick={onClick} className="w-10 h-10 flex justify-center items-center rounded-full bg-primary-700 text-lg">
      S
    </div>
  );
}
